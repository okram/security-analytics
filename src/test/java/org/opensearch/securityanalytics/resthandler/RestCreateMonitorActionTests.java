/*
 * Copyright OpenSearch Contributors
 * SPDX-License-Identifier: Apache-2.0
 */

package org.opensearch.securityanalytics.resthandler;

public class RestCreateMonitorActionTests {//extends OpenSearchRestTestCase {

    public void testPluginIsLoaded() throws Exception {
       /* final Map<String, List<NodesInfoAction>> nodesInfo =  (Map) entityAsMap(client().performRequest(new Request("GET", "_nodes/plugins")));
        nodesInfo.values().forEach(ni -> {
        });
      /* for (final NodeInfo ni : nodesInfo) {
            val plugins = nodeInfo["plugins"] as List<Map<String, Any>>
            for (plugin in plugins) {
                if (plugin["name"] == "opensearch-alerting") {
                    return
                }
            }
        }*/
        //    fail("Plugin not installed");
    }
}
